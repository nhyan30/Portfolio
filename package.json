{
  "name": "react-webpack-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "react-scripts start",
    "build": "cross-env GENERATE_SOURCEMAP=false react-scripts build",
    "build:obfuscate": "npm run build && npx javascript-obfuscator build/static/js/*.js --output build/static/js/ --config obfuscator.config.json"
  }
}
